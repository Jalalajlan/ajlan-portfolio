import ReactDOM from "react-dom/client";
import JalalPortfolioApp from "./App.tsx";
import React from "react";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <JalalPortfolioApp />
  </React.StrictMode>
);
